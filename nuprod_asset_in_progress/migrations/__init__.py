# Scripts de reprise hors flux courant (non importés au chargement du module).
# Le contenu de ce dossier n'est PAS exécuté automatiquement par Odoo : il ne
# s'agit pas de migrations versionnées (migrations/<version>/pre|post-*.py) mais
# d'un utilitaire lançable manuellement via `odoo shell` (cf. reprise_stock_historique).
